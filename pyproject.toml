[project]
name = "brokers"
version = "0.1.0"
description = ""
authors = [
    {name = "с",email = "n-armstrong@mail.ru"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx (>=0.28.1,<0.29.0)",
    "pytest (>=9.1.0,<10.0.0)",
    "kafka-python (>=3.0.1,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
